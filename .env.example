# Rename this file to `.env` and fill in the values. Vite will expose any VITE_ prefixed vars to the client.
# NEVER commit real service role keys. Use only the public anon key below.

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
